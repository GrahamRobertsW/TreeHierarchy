TreeHierararchy
This is a ssimple module that contains the structure for a recursively defined classification tree.
Includes the structure, with ability to fit and predict with the tree, as well as the ability to encode to and from json.

Install:
   python3 -m pip install -e .

